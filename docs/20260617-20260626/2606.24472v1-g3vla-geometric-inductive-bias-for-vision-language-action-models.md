---
title: "G$^3$VLA: Geometric inductive bias for Vision-Language-Action Models"
authors: "Yue Peng, Yongzhe Zhao, Artur Habuda, Khuyen Pham, Yanheng Zhu, Tran Nguyen Le, Fares Abu-Dakka, Li Guo"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24472v1"
tags: ["query:robotics"]
score: 9.0
evidence: geometric inductive bias for vision-language-action models in generalist manipulation
tldr: "Vision-language-action models process multi-camera images independently, ignoring calibrated geometry. G$^3$VLA injects camera-aware geometric biases via ray embeddings, projective positional encoding (PRoPE), and cross-view fusion. It improves task success across LIBERO, RoboCasa24, RoboTwin2.0, and real-robot settings, especially on spatially demanding tasks. The approach requires no depth sensors or manual annotations."
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24472v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 2922, \"height\": 1570}, {\"url\": \"assets/figures/arxiv/2606.24472v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 4401, \"height\": 2202}, {\"url\": \"assets/figures/arxiv/2606.24472v1/fig-003.webp\", \"caption\": \"\", \"page\": 17, \"index\": 3, \"width\": 3410, \"height\": 2090}, {\"url\": \"assets/figures/arxiv/2606.24472v1/fig-004.webp\", \"caption\": \"\", \"page\": 17, \"index\": 4, \"width\": 1760, \"height\": 924}, {\"url\": \"assets/figures/arxiv/2606.24472v1/fig-005.webp\", \"caption\": \"\", \"page\": 18, \"index\": 5, \"width\": 1566, \"height\": 518}, {\"url\": \"assets/figures/arxiv/2606.24472v1/fig-006.webp\", \"caption\": \"\", \"page\": 18, \"index\": 6, \"width\": 1494, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2606.24472v1/fig-007.webp\", \"caption\": \"\", \"page\": 19, \"index\": 7, \"width\": 1499, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2606.24472v1/fig-008.webp\", \"caption\": \"\", \"page\": 19, \"index\": 8, \"width\": 1590, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2606.24472v1/fig-009.webp\", \"caption\": \"\", \"page\": 19, \"index\": 9, \"width\": 1359, \"height\": 549}]"
motivation: "Existing VLA models treat multi-camera inputs as independent 2D images, discarding known camera intrinsics and extrinsics."
method: "G$^3$VLA augments visual tokens with intrinsic-conditioned ray embeddings, projective positional encoding (PRoPE), and bidirectional cross-view fusion."
result: "Consistent performance gains on LIBERO, RoboCasa24, RoboTwin2.0, and real-robot tasks, with largest improvements on spatially and object-sensitive tasks."
conclusion: Geometric transfer is most effective when geometry-aware tokens have direct access to the action generation pathway.
---

## Abstract
Vision-language-action (VLA) models have made rapid progress in generalist robot manipulation by harnessing semantic knowledge from pretrained vision-language backbones, but their visual tokens remain grounded in 2D image coordinates rather than the calibrated geometry of the robot's cameras -- a mismatch especially pronounced in multi-camera setups, where views are coupled by known intrinsics and extrinsics yet processed as independent images. We propose G$^3$VLA, a camera-aware geometric module that injects calibrated structure into the visual-token stream of a pretrained VLA without altering its action space or imitation objective, combining intrinsic-conditioned ray embeddings, projective positional encoding (PRoPE), and bidirectional cross-view fusion. Geometric supervision is provided either from ground-truth point maps when available, or from confidence-gated $π^3$X teacher predictions, requiring no depth sensors or manual annotations. Instantiated on $π_0$, G$^3$VLA yields consistent gains across the LIBERO suites, RoboCasa24, RoboTwin2.0, and real-robot settings, with the largest improvements on spatially and object-sensitive tasks. We further validate on $π_{0.5}$ and GR00T 1.5, with results suggesting that geometric transfer is most effective when geometry-aware tokens have direct access to the action generation pathway. Our project page is at https://sites.google.com/view/g3vla

---

## Detailed Summary (auto-generated)

# Structured Summary of G^3VLA

## 1. Core Problem and Overall Meaning

Vision‑Language‑Action (VLA) models for robot manipulation combine semantic understanding from pretrained vision‑language backbones with action prediction. However, their visual tokens remain tied to 2D image coordinates, ignoring the calibrated camera intrinsics and extrinsics that geometrically couple multiple views. This mismatch is especially pronounced in multi‑camera setups, where each stream is processed independently. The goal of G³VLA is to inject explicit camera geometry into the visual‑token stream of a frozen VLA without changing its action space or imitation objective, thereby improving spatial precision for manipulation tasks.

## 2. Methodology

- **Core idea**: Augment the visual tokens of a pretrained VLA with three geometric modules that use only the camera calibration (intrinsics \(K_v\) and extrinsics \(T_v\)). The backbone, action space, and flow‑matching loss remain unchanged.
- **Key components**:
  - *Intrinsic‑conditioned ray embeddings*: For each patch token, compute the back‑projected viewing direction from \(K_v^{-1}\) and add a learned embedding to the token.
  - *Projective Positional Encoding (PRoPE)*: Derives fixed projective transforms from \(K_v\) and \(T_v\) that bias cross‑view attention with calibrated geometry rather than appearance.
  - *Bidirectional cross‑view fusion*: First applies frame‑attention within each view, then flattens all views for global cross‑view attention using PRoPE.
- **Training**:
  - **Stage 1 – Geometric module pretraining**: Only the new geometry modules and an auxiliary point head are trained, with a dense distillation loss from ground‑truth point maps or confidence‑gated π³X teacher predictions (no depth sensors needed). The backbone is frozen.
  - **Stage 2 – Full policy fine‑tuning**: All parameters are unfrozen, the action loss dominates, and the distillation objective acts as a weak regularizer. The auxiliary head is discarded at inference.
- **Formulas**: Ray coordinate \(R_v(x,y) = [\tilde{r}_v(x,y,1)]_{1:2}\) with \(\tilde{r}_v = K_v^{-1}u\); token update \(z'_{v,p} = z_{v,p} + G_\phi(R_v)_p\); distillation loss \(L_{\text{distill}}\) is a masked MSE over ray coordinates and log‑depth.

## 3. Experiments

- **Datasets / Scenarios**:
  - **LIBERO** (four suites: Goal, Spatial, Object, LIBERO‑10) – tabletop manipulation, measures generalization to spatial relations and object variation.
  - **RoboCasa24** – diverse household kitchen tasks.
  - **RoboTwin2.0** – bimanual handover block task.
  - **Real‑world** – bimanual UR5 bench with two tasks: Pick‑and‑Place Test Tube and Pouring Nut. Evaluated on in‑distribution and out‑of‑distribution camera viewpoints.
- **Baselines**: π₀ (main backbone), π₀.₅, and GR00T 1.5. Each baseline is compared against G³VLA variants with ground‑truth (GT) or π³X‑distilled supervision.
- **Ablations**: w/o ray embeddings, w/o PRoPE, one‑stage training, supervision source (GT vs. π³X).

## 4. Compute / Resources

- Training hyperparameters: Stage 1 (5k steps, λ_act=0.1, λ_distill=1.0), Stage 2 (30k steps, λ_act=1.0, λ_distill=0.05). Batch size 32, bfloat16, AdamW with cosine decay. No explicit GPU count or total training time is provided.

## 5. Experiment Breadth

- **Runs**: All simulation results are averaged over three independent evaluation runs with standard deviations reported.
- **Ablations**: Three axes (components, training curriculum, supervision source) are systematically ablated on LIBERO with π₀. The ablation confirms that each component contributes and that two‑stage training and GT supervision yield the best results.
- **Coverage and fairness**: The method is tested on three distinct VLA backbones, multiple simulation benchmarks, and a real‑robot setup with OOD viewpoints. Baselines are reproduced where possible, and variability is reported. Comparisons are made against official results where available (π₀.₅). The evaluation is thorough and fair.

## 6. Main Conclusions and Findings

- G³VLA consistently improves success rates over baseline π₀ on LIBERO (+3.5 pts average, largest gains on Object and Spatial suites), RoboCasa24 (+2.9 pts), and RoboTwin2.0 (+5 pts with GT).
- On the stronger π₀.₅ backbone, gains are smaller but still positive (+1.1 pts average), confirming compatibility.
- On GR00T 1.5 (two‑tower architecture with indirect cross‑attention to the VLM), gains are mixed, suggesting that geometric injection is most effective when geometry‑aware tokens directly feed the action pathway.
- Real‑world experiments show significant improvements on OOD camera viewpoints, especially in the pouring task.
- Ablations indicate that ray embeddings and PRoPE are complementary, two‑stage training stabilizes learning, and GT supervision outperforms π³X distillation, but π³X still provides a practical alternative when depth is unavailable.

## 7. Strengths

- **Lightweight and backbone‑preserving**: No changes to the VLA action space or imitation objective; calibration is injected only into the visual‑token stream.
- **No depth sensors required**: The π³X teacher provides geometric supervision from RGB alone.
- **Multi‑architecture validation**: Tested on π₀, π₀.₅, and GR00T 1.5, revealing architecture‑dependent effects.
- **Comprehensive evaluation**: Includes simulated benchmarks with diverse task families, real‑robot experiments with OOD generalization, and thorough ablations.
- **Practical usefulness**: Gains are largest on the most spatially demanding tasks, and real‑robot recovery behaviors are observed.

## 8. Limitations

- **Assumes accurate calibration**: Sensitive to calibration drift, synchronization errors, and train‑test mismatch.
- **Teacher quality**: π³X predictions remain imperfect under occlusions, specularities, blur, or weak‑prior viewpoints; gating mitigates but does not eliminate bias.
- **Architecture dependence**: Gains are attenuated on GR00T 1.5 due to its two‑tower design, indicating that geometry‑aware tokens must directly reach action generation.
- **Offline cost**: Teacher cache generation and auxiliary‑head training add offline computational overhead (neither is needed at deployment).
- **Scope**: Does not address failures originating from limited demonstrations, action‑space constraints, or weak language‑action grounding.
- **Real‑world validation**: Only two tasks with a single bimanual setup; broader real‑world evaluation is needed.

(End)
