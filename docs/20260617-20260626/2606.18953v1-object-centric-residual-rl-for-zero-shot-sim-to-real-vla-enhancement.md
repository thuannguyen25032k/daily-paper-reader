---
title: Object-Centric Residual RL for Zero-Shot Sim-to-Real VLA Enhancement
authors: "Kinam Kim, Namiko Saito, Heecheol Kim, Katsushi Ikeuchi, Jaegul Choo, Yasuyuki Matsushita"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18953v1"
tags: ["query:robotics"]
score: 9.0
evidence: object-centric residual RL for zero-shot sim-to-real VLA enhancement
tldr: "VLA models fail in precise physical interactions due to compounding errors. We propose object-centric residual RL trained in simulation with pose noise and dropout, transferring zero-shot to real robots. On a Franka FR3, success rate improves from 42% to 76% across five tasks. Improved rollouts can further retrain the base VLA for self-improvement without additional teleoperation."
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18953v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 2538, \"height\": 1076}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-002.webp\", \"caption\": \"\", \"page\": 3, \"index\": 2, \"width\": 437, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-003.webp\", \"caption\": \"\", \"page\": 3, \"index\": 3, \"width\": 460, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-004.webp\", \"caption\": \"\", \"page\": 3, \"index\": 4, \"width\": 436, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-005.webp\", \"caption\": \"\", \"page\": 6, \"index\": 5, \"width\": 2603, \"height\": 697}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-006.webp\", \"caption\": \"\", \"page\": 7, \"index\": 6, \"width\": 730, \"height\": 612}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-007.webp\", \"caption\": \"\", \"page\": 7, \"index\": 7, \"width\": 559, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-008.webp\", \"caption\": \"\", \"page\": 13, \"index\": 8, \"width\": 2560, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-009.webp\", \"caption\": \"\", \"page\": 13, \"index\": 9, \"width\": 2560, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-010.webp\", \"caption\": \"\", \"page\": 13, \"index\": 10, \"width\": 2560, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-011.webp\", \"caption\": \"\", \"page\": 13, \"index\": 11, \"width\": 2560, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-012.webp\", \"caption\": \"\", \"page\": 13, \"index\": 12, \"width\": 2560, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-013.webp\", \"caption\": \"\", \"page\": 18, \"index\": 13, \"width\": 5329, \"height\": 3732}, {\"url\": \"assets/figures/arxiv/2606.18953v1/fig-014.webp\", \"caption\": \"\", \"page\": 19, \"index\": 14, \"width\": 1592, \"height\": 912}]"
motivation: Vision-Language-Action policies are brittle in precise physical interactions; existing residual RL methods face sim-to-real dilemmas and real-world RL is costly.
method: "Object-centric residual RL refines VLA actions using object poses, trained purely in simulation with pose noise injection and dropout, then transferred zero-shot."
result: "On a real Franka FR3 robot, success rate increases from 42% to 76% zero-shot across five manipulation tasks."
conclusion: "Zero-shot sim-to-real transfer of residual RL enhances VLA robustness, and improved rollouts enable self-improvement without extra teleoperation."
---

## Abstract
Vision-Language-Action (VLA) models can generalize across diverse manipulation tasks, but their imitation-learning-based policies remain brittle in precise physical interactions due to compounding execution errors; Can a reinforcement learning policy trained purely in simulation improve the robustness of real-world VLAs zero-shot? Residual RL, which learns a corrective policy on top of a frozen VLA, offers a natural framework, but existing approaches face a fundamental sim-to-real dilemma: privileged-state methods require lossy distillation for deployment; image-based methods suffer from the visual domain gap; and real-world RL is costly and unsafe. We propose an object-centric residual RL framework that refines VLA actions using object poses, enabling a compact observation space that transfers consistently between simulation and reality. To align the two domains, we additionally replay the same teleoperation demonstrations in simulation to train a sim counterpart of the real-world VLA. The residual RL policy is trained only in simulation with pose noise injection and dropout, and transfers zero-shot to the real robot. Across five manipulation tasks on a real Franka Research 3 (FR3) robot, our method improves the success rate from 42% to 76% zero-shot, and the improved rollouts can be further reused to retrain the base VLA for self-improvement without additional teleoperation. Project page: https://www.microsoft.com/en-us/research/articles/object-centric-residual-rl/

---

## Detailed Summary (auto-generated)

## Summary of "Object-Centric Residual RL for Zero-Shot Sim-to-Real VLA Enhancement"

### 1. Core Problem and Overall Meaning

- **Motivation**: Vision-Language-Action (VLA) models are powerful for diverse manipulation but suffer from compounding execution errors due to imitation learning, making them brittle in precise physical interactions.
- **Background**: Residual RL (learning a corrective policy on top of a frozen base) is a natural solution, but existing approaches face a sim-to-real dilemma:
  - Privileged-state methods require lossy distillation for deployment.
  - Image-based methods suffer from the visual domain gap.
  - Real-world RL is costly, unsafe, and requires extensive infrastructure.
- **Core Meaning**: The paper proposes a method to train a residual RL policy entirely in simulation using object-centric observations (6-DoF poses), enabling zero-shot transfer to real robots without distillation, real-world RL, or complex reconstruction pipelines.

### 2. Methodology

- **Core Idea**: Use object poses (6-DoF) as the primary observation space for the residual policy, since poses can be reliably estimated in both simulation and reality via off-the-shelf estimators (FoundationPose + SAM2), avoiding visual domain gaps.
- **Key Technical Details**:
  - **Stage 1 – Paired Sim/Real VLA**: Teleoperation demonstrations are collected on the real robot and replayed in simulation to train a sim VLA alongside the real VLA using the same action data. This aligns action distributions across domains.
  - **Stage 2 – Object-Centric Residual RL**:
    - Observation: \( s_t = [s^{obj}_t, s^{prop}_t, a^{base}_t] \), where \( s^{obj}_t \) is 6-DoF object pose.
    - Action: \( a_t = a^{base}_t \oplus \pi^{sim}_{res}(s_t) \) (addition for position/gripper, quaternion multiplication for rotation).
    - Training: TD3 with clipped exploration noise, dense rewards, and two robustness augmentations:
      - Pose noise injection: \( \hat{p}_{obj} = p_{obj} + \epsilon_p, \quad \hat{q}_{obj} = q_{obj} \otimes \epsilon_q \), with hierarchical uniform sampling.
      - Pose dropout: with probability \( \rho_{drop} \), zero out the object pose vector, forcing the policy to rely on proprioception and base action.
  - **Stage 3 – Zero-Shot Deployment**: Frozen real VLA + frozen sim-trained residual. At each timestep, if the pose estimator’s confidence \( c_t < \tau_c \), the pose input is zeroed out to trigger the learned dropout fallback.

### 3. Experiments

- **Datasets/Scenarios**: Five tabletop manipulation tasks on a real Franka Research 3 (FR3) robot and in MuJoCo simulation:
  - Cube Lift (Lift)
  - Pick-and-Place (PnP)
  - Stack Cube (Stack)
  - Close Drawer (Close)
  - Stand Cup Up (Stand)
  - 30 teleoperation demonstrations per task for sim/real VLA fine-tuning.
- **Benchmarks**: Base VLAs: GR00T-N1.5 (primary) and π0.5 (for generalization test). Baselines: Distillation-based residual RL (from privileged state), image-based residual RL.
- **Baselines Compared**: Distillation-based (privileged state → image student) and image-based residual RL (directly on images).

- **Results**:
  - Real robot: Average success rate improved from **42% to 76%** across five tasks (zero-shot).
  - Each task improved (e.g., Cube Lift: 7/20 → 17/20, Close Drawer: 14/20 → 20/20).
  - Generalization: Residual RL also improved π0.5 (average real success: 30.8/50 → 48.8/50).

### 4. Compute/Resources

- **GPUs/Count**: Not explicitly mentioned (likely a single GPU for training, e.g., NVIDIA RTX 4090 or similar).
- **Training Time**: Not reported explicitly, but the residual policy is a lightweight 2-layer MLP (512 hidden units) trained with TD3 for 50k steps per task, suggesting moderate compute (hours per task).
- **Inference Time**: Residual forward pass ~0.06 ms on GPU; pose estimation (FoundationPose) ~18 ms per frame (run asynchronously, not a control loop bottleneck).
- **Resources**: Simulation environment (MuJoCo) is lightweight; no realistic rendering required for main method.

### 5. Experiment Breadth

- **Number of Runs**: Simulation results reported as mean ± std over 3 seeds. Real-world results: 20 trials per condition (single seed plus ablation baselines).
- **Ablations Covered**:
  - **Robustness training**: Pose dropout vs. noise injection vs. both → combined yields best transfer.
  - **Observation space**: Object-centric vs. distillation-based vs. image-based → object-centric transfers best.
  - **VLA architecture**: GR00T-N1.5 and π0.5 → consistent improvement.
  - **Self-improvement**: SFT on residual-corrected rollouts improves success rate and reduces episode length.
- **Coverage**: Sufficient and fair—the paper provides controlled comparisons, multiple tasks, multiple VLA backbones, and comprehensive ablations.

### 6. Main Conclusions and Findings

- The object-centric residual RL framework enables **zero-shot sim-to-real transfer** without distillation or real-world RL.
- Performance improvement on real robot: 42% → 76% average success rate across five tasks.
- The residual selectively corrects the base VLA when misaligned, leading to 9–22% faster task completion.
- Residual-corrected rollouts can be reused for **VLA self-improvement** (SFT) without additional teleoperation.
- The method is agnostic to the base VLA architecture (demonstrated with both GR00T-N1.5 and π0.5).

### 7. Strengths

- **Zero-shot transfer**: No real-world RL, no distillation, no complex reconstruction pipelines—direct deployment from simulation to reality.
- **Pragmatic observation design**: Object poses are domain-invariant, reliably recoverable in reality, and yield a compact policy (2-layer MLP) that adds negligible compute overhead.
- **Robustness training**: Pose noise and dropout make the policy resilient to real-world perception errors.
- **Self-improvement loop**: The residual-corrected trajectories bootstrap the base VLA, enabling automatic improvement without extra human data.
- **Generalizability**: Works across multiple VLA backbones and five diverse manipulation tasks.

### 8. Limitations

- **Pose estimation failure**: Relies on real-time 6-DoF tracking (FoundationPose + SAM2), which can fail under full occlusion, heavy clutter, or specular reflections. Memory-based pose estimation could help.
- **Manual object specification**: Task-relevant objects must be manually specified; scaling to open-world settings requires automatic identification (e.g., from VLA attention maps).
- **Dynamics gap**: The pose-based observation bridges the visual gap but not the dynamics gap—contact friction and gripper compliance differences between sim and real may cause suboptimal corrections in contact-rich tasks.
- **Residual architecture limits**: The policy can correct mild deviations but cannot recover from states far outside the base VLA’s training distribution.
- **Precision limits**: Tasks requiring sub-millimeter precision or involving very small objects may exceed current pose estimation accuracy.
- **Bias risk**: The simulation environment is manually constructed from measured dimensions; domain shift may affect tasks with many unmodeled physical phenomena.

(End)
