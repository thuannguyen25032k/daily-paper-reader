---
title: "FlowDPG: Deterministic Policy Gradient on Flow Matching Policies for Real-World Manipulation"
authors: "Kexin Shi, Junyao Shi, Poorvi Hebbar, Zhuolun Zhao, Tarun Amarnath, Yifan Su, Shikhar Bahl, Deepak Pathak"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22303v1"
tags: ["query:robotics"]
score: 9.0
evidence: deterministic policy gradient for flow matching policies
tldr: "Problem: Policy gradient for flow matching policies requires costly backpropagation through time (BPTT). Method: FlowDPG uses a DDPG-style distillation of critic gradients into the velocity field, avoiding BPTT. Result: Achieves 92% success on a real-world dual-arm AirPods assembly task. Impact: Enables stable and efficient RL for flow matching policies in robotic manipulation."
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22303v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 1764, \"height\": 992}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-002.webp\", \"caption\": \"\", \"page\": 1, \"index\": 2, \"width\": 1920, \"height\": 1080}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-003.webp\", \"caption\": \"\", \"page\": 1, \"index\": 3, \"width\": 1871, \"height\": 1052}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-004.webp\", \"caption\": \"\", \"page\": 1, \"index\": 4, \"width\": 1920, \"height\": 1080}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-005.webp\", \"caption\": \"\", \"page\": 1, \"index\": 5, \"width\": 1764, \"height\": 993}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-006.webp\", \"caption\": \"\", \"page\": 1, \"index\": 6, \"width\": 1920, \"height\": 1080}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-007.webp\", \"caption\": \"\", \"page\": 1, \"index\": 7, \"width\": 1920, \"height\": 1080}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-008.webp\", \"caption\": \"\", \"page\": 1, \"index\": 8, \"width\": 1807, \"height\": 1016}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-009.webp\", \"caption\": \"\", \"page\": 3, \"index\": 9, \"width\": 586, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-010.webp\", \"caption\": \"\", \"page\": 8, \"index\": 10, \"width\": 1335, \"height\": 609}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-011.webp\", \"caption\": \"\", \"page\": 15, \"index\": 11, \"width\": 1668, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2606.22303v1/fig-012.webp\", \"caption\": \"\", \"page\": 15, \"index\": 12, \"width\": 1644, \"height\": 320}]"
motivation: "BPTT in flow matching policies is computationally prohibitive and numerically fragile, limiting RL for real-world manipulation."
method: "FlowDPG distills critic gradients into the velocity field via a DDPG-style update, bypassing BPTT and combining demonstration and value-driven signals."
result: "On a long-horizon dual-arm AirPods assembly task, FlowDPG achieves 92% end-to-end success, significantly outperforming recent RL methods."
conclusion: "FlowDPG provides a BPTT-free framework for policy improvement on flow matching policies, validated in real-world robotic assembly."
---

## Abstract
Real-world reinforcement learning for robotic manipulation remains challenging, and this difficulty is amplified for flow matching policies: applying policy gradient methods to these policies is fundamentally limited by the need to backpropagate through time(BPTT) along the multi-step ODE that maps noise to actions, which is computationally prohibitive and numerically fragile. We propose FlowDPG, a DDPG-style method specifically designed for flow matching policies that distills the critic gradient into the velocity field at training time, bypassing BPTT entirely. Intuitively, FlowDPG combines two complementary vectors: the demonstration-driven velocity that keeps the action feasible, and the critic-driven correction that steers it toward higher value. Our contributions are threefold: (1) a BPTT-free distillation framework that enables stable DDPG-style policy improvement on flow matching policies, (2) a formal connection between the FlowDPG update direction and vanilla Deterministic Policy Gradient via three explicit approximations, and (3) real-world validation on a long-horizon, multi-stage, dual-arm AirPods assembly task, where FlowDPG attains a 92% end-to-end success rate, substantially outperforming recent RL methods spanning value-conditioning, auxiliary-module adaptation, and adjoint-based critic-gradient approaches. Videos and more results are provided on the project page https://flowdpg.github.io.

---

## Detailed Summary (auto-generated)

# Structured Summary: FlowDPG: Deterministic Policy Gradient on Flow Matching Policies for Real-World Manipulation

## 1. Core Problem and Overall Meaning

- **Problem**: Applying policy gradient methods to flow matching policies (which generate actions via multi-step ODE denoising) requires backpropagation through time (BPTT) across the entire ODE trajectory. This is computationally expensive (cost scales linearly with solver steps) and numerically fragile (gradient explosion/vanishing), especially when the velocity field is not yet well-trained.
- **Meaning**: FlowDPG addresses this bottleneck by enabling stable, BPTT-free DDPG-style policy improvement for flow matching policies, making real-world reinforcement learning feasible for expressive generative action heads in robotic manipulation.

## 2. Methodology

- **Core Idea**: Distill the critic gradient \(\nabla_a Q\) directly into the flow matching velocity field at training time, bypassing BPTT. The update combines a demonstration-driven velocity (keeps actions feasible) with a critic-driven correction (steers toward higher value).
- **Key Technical Details**:
  - **Q-Gradient Estimation**: From any intermediate noisy action \(x_t\), a clean action \(\hat{a} = x_t + (1 - t) \cdot v_\theta(x_t, t, s)\) is computed via a single forward pass of the velocity predictor \(v_\theta\). The critic gradient is then evaluated at \(\hat{a}\) (stop-grad on critic): \(g = \nabla_{\hat{a}} \min_i Q_{\phi_i}(s, \hat{a})\).
  - **Adaptive Shift**: Normalize the gradient by local velocity scale: \(\Delta = \alpha \cdot \frac{\|u_t\|}{\|g\| + \epsilon} \cdot g\) where \(u_t = x_1 - x_0\).
  - **Distilled Target**: \(a^* = \hat{a} + \Delta\), then \(u_t^* = a^* - x_0\). The policy is trained with L2 regression: \(L_{\text{FlowDPG}} = \lambda \cdot \|v_\theta(x_t, t, s) - u_t^*\|^2 + (1 - \lambda) \cdot \|v_\theta(x_t, t, s) - u_t\|^2\). \(\lambda\) warms up linearly over \(N_{\text{warmup}}\) steps.
  - **Consistency Regularizer**: Ensures \(\hat{a}\) is close to the true clean action \(x_1\) via \(L_{\text{cons}} = \mathbb{E}[\|\hat{a} - x_1\|^2]\), which reduces to a weighted velocity-error penalty.
  - **Value Estimation**: Uses IQL with twin critics and a stage-aware reward predictor (SARM) for dense rewards.
- **Theoretical Connection**: The FlowDPG update direction is derived as an approximation of vanilla DPG under three explicit approximations: (i) critic gradient evaluated at \(\hat{a}\) instead of \(x_1\), collapsing the ODE sensitivity matrix to identity; (ii) Monte Carlo sampling over \(t\) instead of trajectory-wide integration; (iii) adaptive scaling replaces unnormalized step size.
- **Algorithm Flow** (Pseudocode in paper):
  1. Pretrain SARM; initialize backbone, actor, critic.
  2. **Offline phase**: For each step, sample batch from offline buffer, compute reward via SARM, sample \(t\), compute \(\hat{a}\), loss, update actor/critic with L2 distillation and consistency regularizer.
  3. **Online phase**: Deploy policy, collect rollouts, append to online buffer. Update actor/critic asynchronously with 1:1 offline/online mixing.

## 3. Experiments

- **Task & Scenario**: Long-horizon, dual-arm AirPods assembly with 8 sequential sub-stages (grasp case, open case, grasp R pod, insert R pod, grasp L pod, insert L pod, close case, place case). Randomized case and pod poses. Two Franka arms; demonstrations via GELLO teleoperation.
- **Evaluation Protocol**: 25 cases (50 pod manipulations). Success at each stage (with retries allowed), unweighted average across stages, and end-to-end episode success (all pods correctly inserted).
- **Baselines**:
  - *Value-conditioning*: RA-BC, AWR, RECAP (use scalar value/advantage only)
  - *Auxiliary-module adaptation*: DSRL, PLD, RLT (train separate adapter on frozen policy)
  - *Critic-gradient*: QAM (adjoint matching, concurrent work)
- **Main Results**: FlowDPG (+online) achieves **92% end-to-end success**, 28% improvement over BC base (64%), 12% over strongest prior (RLT at 80%).
- **Online RL Robustness**: Evaluated on three disturbance families (re-grasp, re-open/close, re-insert, adapt grasp). Offline+Online outperforms offline by up to +18% (adapt grasp from 70% to 88%).
- **Ablations**:
  - Removing consistency regularizer: Overall drops from 92% to 80%.
  - Removing adaptive shift: Drops to 76%.
  - Removing both: further marginal drop to 72%.
  - Reward component ablation: Removing progress term → 80%; removing stage-transition term → 76%; removing both → 60% (below BC base).

## 4. Compute/Resources

- **Not explicitly stated**: The paper does not mention GPU count, training time, or hardware details for compute. Only mention of "real-world" deployment on Franka arms. This is a limitation in reproducibility for compute.

## 5. Experiment Breadth

- **Runs**: Main results reported on 25 cases (50 pod manipulations) – moderate sample size. No explicit number of seeds or multiple random runs mentioned.
- **Ablations**: Comprehensive on key design choices (consistency, adaptive shift, reward components). Also includes disturbance robustness tests.
- **Fairness**: All baselines share backbone, action horizon, reward, value head, and data split – ensures fair comparison.
- **Coverage**: Single task family (dual-arm assembly) and single platform (Franka). Sufficient for proof-of-concept but limited for generalization claims.

## 6. Main Conclusions and Findings

- FlowDPG achieves **92%** end-to-end success on a challenging real-world bimanual assembly task, significantly outperforming prior RL methods that either do not use critic gradients or use BPTT-based approaches.
- The BPTT-free distillation is **stable** and **computationally efficient** (one extra forward pass + one critic gradient per update).
- Online RL phase improves robustness to unseen disturbances, especially scene perturbations.
- Ablations confirm the necessity of the consistency regularizer and adaptive shift for reliable critic gradients.

## 7. Strengths

- **Elegant Solution**: Avoids BPTT entirely, making RL fine-tuning feasible for flow matching policies without expensive ODE backpropagation.
- **Theoretical Grounding**: Explicit derivation connecting FlowDPG to vanilla DPG via three clear approximations.
- **Real-World Validation**: Demonstrated on a long-horizon, contact-rich, dual-arm task with millimeter precision – a practical and challenging benchmark.
- **Comprehensive Ablations**: Detailed mechanism-level analysis (projection error, Q-value discrepancy, training loss) supports each design choice.
- **Reproducible Ingredients**: Uses public components (DINOv2, DiT, IQL, SARM) and all baselines share same backbone.

## 8. Limitations

- **Average-Only Consistency**: The consistency regularizer minimizes projection error only on average; some states may still have unreliable critic gradients, which are not detected or down-weighted.
- **Reward Dependence**: Reward formulation requires stage-level annotations (SARM), limiting applicability to tasks without clear sub-stage decomposition.
- **Single Platform & Task**: Evaluation is on one bimanual manipulator and one task family. Generalization to larger VLA policies or different contact dynamics is left to future work.
- **Compute Details Missing**: No information on GPU hours, training time, or hardware, which hinders reproducibility assessment.
- **Limited Statistical Rigor**: Results are reported on a single set of 25 cases without multiple random seeds or confidence intervals.

(End)
