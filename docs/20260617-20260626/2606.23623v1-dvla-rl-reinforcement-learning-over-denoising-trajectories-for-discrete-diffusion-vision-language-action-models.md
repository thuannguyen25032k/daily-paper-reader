---
title: "dVLA-RL: Reinforcement Learning over Denoising Trajectories for Discrete Diffusion Vision-Language-Action Models"
authors: "Yuhao Wu, Yitian Liu, Weijie Shen, Mishuo Han, Wenjie Xu, Haotian Liang, Zhongshan Liu, Yinan Mao, Lei Xu, Xinping Guan, Ru Ying, Ran Zheng, Wei Sui, Xiaokang Yang, Wenbo Ding, Yao Mu"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23623v1"
tags: ["query:robotics"]
score: 9.0
evidence: reinforcement learning for discrete diffusion VLA models
tldr: "Reinforcement learning over denoising trajectories is proposed for discrete diffusion vision-language-action models, addressing the intractability of marginal action probability by optimizing joint path probabilities via a Markov decision process. A unified step scheduling adapts denoising steps to task complexity. Achieves 99.7% success on LIBERO and 30.6% improvement on RoboTwin 2.0 over supervised fine-tuning."
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23623v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 1019, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-002.webp\", \"caption\": \"\", \"page\": 2, \"index\": 2, \"width\": 1744, \"height\": 1098}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-003.webp\", \"caption\": \"\", \"page\": 2, \"index\": 3, \"width\": 2473, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-004.webp\", \"caption\": \"\", \"page\": 2, \"index\": 4, \"width\": 2474, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-005.webp\", \"caption\": \"\", \"page\": 2, \"index\": 5, \"width\": 627, \"height\": 627}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-006.webp\", \"caption\": \"\", \"page\": 6, \"index\": 6, \"width\": 354, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-007.webp\", \"caption\": \"\", \"page\": 6, \"index\": 7, \"width\": 354, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-008.webp\", \"caption\": \"\", \"page\": 6, \"index\": 8, \"width\": 355, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-009.webp\", \"caption\": \"\", \"page\": 6, \"index\": 9, \"width\": 354, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-010.webp\", \"caption\": \"\", \"page\": 6, \"index\": 10, \"width\": 627, \"height\": 627}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-011.webp\", \"caption\": \"\", \"page\": 6, \"index\": 11, \"width\": 1254, \"height\": 1254}, {\"url\": \"assets/figures/arxiv/2606.23623v1/fig-012.webp\", \"caption\": \"\", \"page\": 6, \"index\": 12, \"width\": 1024, \"height\": 1536}]"
motivation: "The marginal action probability in discrete diffusion VLAs is intractable, hindering reinforcement learning for policy refinement."
method: "We model the denoising process as an MDP and optimize the joint probability of the sampled generation path, with unified step scheduling for multi-task learning."
result: "Achieves 99.7% success rate on LIBERO and 30.6% improvement over SFT on RoboTwin 2.0, competitive with strong baselines."
conclusion: "dVLA-RL effectively enables RL for discrete diffusion VLAs, boosting performance through trajectory-level optimization and adaptive denoising steps."
---

## Abstract
Vision-Language-Action (VLA) models have established a powerful paradigm for generalist robotic manipulation by grounding control into the semantic reasoning of VLMs. Prevailing architectures typically model actions continuously via diffusion or flow processes, or discretely through either autoregressive generation or parallel decoding. Recently, Discrete Diffusion VLAs (dVLAs) have emerged as a distinct alternative, unifying vision, language, and action into a single discrete token space via masked generative modeling. While combining iterative refinement with unified representations, its training has thus far been restricted to Supervised Fine-Tuning (SFT), leaving the potential of Reinforcement Learning (RL) for further policy refinement largely unexplored. A fundamental challenge in RL for dVLAs is that the marginal probability of the final action generated by dVLAs remains intractable. To solve this problem, we propose \textbf{dVLA-RL}, shifting the learning objective from the marginal action probability to the joint probability of the sampled generation path. Specifically, by modeling the denoising process as a Markov Decision Process (MDP), we mathematically formulate this path probability as a product of step-wise transitions. This trajectory-level objective provides a unified formulation that natively accommodates variable denoising steps. Leveraging this intrinsic fexibility, we introduce a unified step scheduling approach for complex multi-task learning, tailoring denoising steps to specific task complexities to maximize both success rates and computational effciency. Extensive evaluations demonstrate that our approach achieves a success rate of \textbf{99.7\%} on LIBERO. Furthermore, it establishes strong VLA-based results on RoboTwin 2.0 by delivering a \textbf{30.6\%} improvement over the SFT baseline, remaining competitive with strong World-Action Model baselines.

---

## Detailed Summary (auto-generated)

## Summary of dVLA-RL: Reinforcement Learning over Denoising Trajectories for Discrete Diffusion Vision-Language-Action Models

### 1. Core Problem and Overall Meaning
- **Motivation:** Discrete diffusion Vision-Language-Action (dVLA) models unify vision, language, and action in a discrete token space via masked denoising. However, their training has been limited to supervised fine-tuning (SFT). Applying policy-gradient reinforcement learning (RL) is difficult because the marginal probability of the final action is intractable—it requires summing over all possible intermediate denoising paths.
- **Key Insight:** The paper proposes to bypass the intractable marginal probability by optimizing the joint probability of the **sampled denoising trajectory** (the actual path taken during inference), which factorizes tractably over Markovian steps.
- **Overall Meaning:** dVLA-RL is the first framework to enable standard PPO-style RL for native discrete diffusion VLA policies, achieving state-of-the-art results on robotic manipulation benchmarks.

### 2. Methodology
- **Core Idea:** Treat the multi-step denoising process as a Markov Decision Process (MDP). Instead of computing the marginal action probability \(\pi_\theta(a_t|s_t)\), optimize the joint probability of the executed denoising path:  
  \[
  \pi_\theta(\tau|s_t) = \prod_{k=1}^{K} P_\theta(x_{k-1}|x_k, s_t)
  \]
  where each transition includes a schedule-based unmasking step and a token-prediction step.
- **Tractable Formulation:** The log-probability of the trajectory is expressed as:
  \[
  \log \pi_\theta(\tau|s_t) = \sum_{k=K}^{1} \left[ \log P_{\text{unmask}}(U_k|p_\theta, k) + \sum_{i \in U_k} \log p_\theta(x_{k-1}^i | x_k, s_t) \right]
  \]
- **Implicit Unmasking Optimization:** The unmasking probability term is dropped because its gradient is unstable and negligible under the PPO trust region. The loss is computed only on **newly denoised tokens** (subset \(M_k \subseteq U_k\) that transition from [MASK] to a token ID).
- **Final Objective:**
  \[
  L_{\text{dVLA-RL}}(\theta) = \mathbb{E}_\tau \left[ \sum_{k=K}^{1} \sum_{i \in M_k} \log p_\theta(x_{k-1}^i | x_k, s_t) \cdot A_t \right]
  \]
  where \(A_t\) is the advantage.
- **Hybrid Step Scheduling:** Tasks are assigned different numbers of denoising steps (1, 2, or 4) according to their initial SFT performance, leveraging the unified trajectory-level framework to balance action quality, sample efficiency, and computational cost.

### 3. Experiments
- **Benchmarks:**
  - **LIBERO** (single-arm): 4 suites (Spatial, Object, Goal, Long) with 500 demonstrations per suite.
  - **RoboTwin 2.0** (bimanual): 8 tasks with 1,000 demonstrations per task.
- **Backbone:** MM-ACT (discrete diffusion VLA), action-only SFT, action chunk size 16.
- **Baselines:**
  - VLA-based: OpenVLA, π0, π0.5, UniVLA, SimpleVLA-RL, MM-ACT*.
  - World-Action Model (WAM) based: Motus, Cosmos Policy, LingBot-VA, Fast-WAM (gray rows for reference, not strictly controlled).
- **Evaluation:** Sparse outcome reward (1 for success, 0 otherwise). Best online rollout success rates reported. 50 trials per LIBERO subtask, 100 per RoboTwin task for SFT; 512 rollouts per step during RL.

### 4. Compute/Resources
- **Training time** (wall-clock per optimization step, Table 3):  
  - 1-step: 259.33 s  
  - 2-step: 452.06 s  
  - 4-step: 845.95 s  
  - Hybrid: 607.89 s  
- **Inference delay** (per action chunk): 182.47 ms (1-step), 364.21 ms (2-step), 728.92 ms (4-step), 387.24 ms (Hybrid).  
- **Inference FLOPs:** approximately proportional to steps.  
- **Hardware details** (number of GPUs, etc.) are not specified in the paper.

### 5. Experiment Breadth
- **Number of runs:** Each RL experiment is reported as the best success rate during training; training curves include standard deviation (shaded regions) and exponential moving averages.
- **Ablations:**
  1. **Trajectory-level vs. last-step-only** probability modeling on LIBERO-Spatial and LIBERO-Object under 2-step and 4-step denoising (Figure 3).
  2. **Denoising step length** (1-step, 2-step, 4-step, and Hybrid) on RoboTwin 2.0 (Figure 4).
  3. **Sample efficiency** comparison of denoising configurations (Figure 1c).
- **Fairness:** Comparisons with baselines are reported under similar SFT data scales; WAM baselines use different training settings and are contextualized separately. Ablations share the same backbone and reward settings.

### 6. Main Conclusions and Findings
- **dVLA-RL consistently improves over SFT** on both benchmarks, achieving **99.7% average success rate on LIBERO** and **30.6% absolute improvement on RoboTwin 2.0** (61.4% → 92.0%).
- **Trajectory-level probability modeling is necessary**; the last-step-only proxy yields unstable or lower final performance (Figure 3).
- **Hybrid step scheduling** (task-adaptive denoising steps) achieves the best balance of sample efficiency, training cost, and success rate (e.g., 0.920 peak vs. 0.908 for fixed multi-step and 0.885 for 1-step on RoboTwin 2.0).
- dVLA-RL achieves state-of-the-art among VLA methods and remains competitive with World-Action Models.

### 7. Strengths
- **Novel formulation:** Resolves the intractable marginalization problem for discrete diffusion policies by tractably optimizing the actual denoising path.
- **Practical efficiency:** Hybrid step scheduling reduces computational overhead while preserving multi-step refinement benefits.
- **Strong empirical results:** Top performance on LIBERO and substantial gains on the challenging RoboTwin 2.0 bimanual benchmark.
- **Clean ablation design:** Directly isolates the effect of trajectory-level modeling, denoising length, and hybrid strategy under the same experimental setting.

### 8. Limitations
- **Simulation-only evaluation:** Experiments are conducted entirely in simulation; real-world deployment and generalization are not addressed.
- **Backbone specificity:** The method is implemented on a single model architecture (MM-ACT); applicability to other discrete diffusion VLA architectures is not verified.
- **Hybrid step selection criterion:** Task-specific step assignment is based on initial SFT performance; a more adaptive or automated selection method could be explored.
- **Limited hardware reporting:** No GPU counts or total training wall-clock times are provided, limiting reproducibility assessment.
- **Bias in WAM comparisons:** WAM baselines follow different training protocols and are not strictly controlled; performance comparisons should be interpreted with caution.

(End)
