---
title: Flatness Preserves Instruction Following in Vision-Language-Action Models
authors: "Haochen Zhang, Yonatan Bisk"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23641v1"
tags: ["query:robotics"]
score: 9.0
evidence: flatness-preserving optimization to prevent instruction blindness in VLA models
tldr: "Vision-language-action (VLA) models finetuned on limited robot data suffer instruction blindness, ignoring language instructions and relying on visual shortcuts. To address this, we hypothesize instruction blindness arises from sharp loss landscapes and propose applying sharpness-aware minimization during finetuning to preserve flatness. This flatness-preserving optimization improves instruction following by over 60% across multiple simulation and real-world benchmarks without additional data, architectural changes, or retraining. Our analysis further shows selective sharpness effects and complementarity to existing guidance techniques."
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23641v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 1894, \"height\": 674}, {\"url\": \"assets/figures/arxiv/2606.23641v1/fig-002.webp\", \"caption\": \"\", \"page\": 7, \"index\": 2, \"width\": 1892, \"height\": 492}, {\"url\": \"assets/figures/arxiv/2606.23641v1/fig-003.webp\", \"caption\": \"\", \"page\": 8, \"index\": 3, \"width\": 1914, \"height\": 626}, {\"url\": \"assets/figures/arxiv/2606.23641v1/fig-004.webp\", \"caption\": \"\", \"page\": 15, \"index\": 4, \"width\": 936, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2606.23641v1/fig-005.webp\", \"caption\": \"\", \"page\": 17, \"index\": 5, \"width\": 1080, \"height\": 1440}]"
motivation: "Standard finetuning on limited robot data creates sharp minima, causing instruction blindness where VLA models ignore language instructions."
method: "Apply sharpness-aware minimization during VLA finetuning to learn flatter loss landscapes, making models robust to weight perturbations."
result: "Improves instruction following by over 60% across multiple simulation and real-world benchmarks without additional data or architectural changes."
conclusion: Flatness-aware finetuning effectively mitigates instruction blindness in VLA models and is complementary to existing guidance techniques.
---

## Abstract
Vision-language-action (VLA) models have the potential for open-world generalization by leveraging pretrained vision-language representations, yet downstream finetuning on limited robot data often degrades these representations, leading to brittle policies that ignore language instructions in favor of visual shortcuts, a failure mode we term instruction blindness. We hypothesize that standard finetuning with limited data applies gradients to a sparse set of points, which manifests as a sharp loss landscape with high-curvature minima. We propose to address this directly through flatness-preserving optimization while finetuning on the exact same data, where learning a flatter landscape results in a model more robust to perturbations in the weight space. Specifically, we demonstrate that simply applying sharpness-aware minimization during VLA finetuning significantly improves instruction following by over 60% across multiple simulation and real-world benchmarks without additional data, architectural modification, or retraining. We further analyze the effect of selective sharpness, quantify its effects, and show that our approach is complementary to existing guidance techniques. Project page can be found at https://haochenz11.github.io/papers/flatness-vla/.

---

## Detailed Summary (auto-generated)

### Core Problem and Overall Meaning (Motivation and Background)

- **Problem**: Vision-language-action (VLA) models finetuned on limited robot data suffer from *instruction blindness*—they ignore language instructions and rely on visual shortcuts (e.g., picking the training object regardless of the command). This failure mode arises because standard finetuning on a sparse, biased dataset overfits to a few samples.
- **Root Cause**: The loss landscape becomes sharp (high-curvature minima), leading to brittle embeddings that lose sensitivity to language variations.
- **Goal**: Improve instruction following without additional data, architectural changes, or retraining, by preserving flatness in the loss landscape during finetuning.

### Methodology: Core Idea, Key Technical Details, Algorithm Flow

- **Core idea**: Apply Sharpness-Aware Minimization (SAM) during VLA finetuning to find flatter minima. Flat minima are more robust to weight perturbations and better preserve pretrained vision-language representations.
- **SAM algorithm** (two-step gradient update per iteration):
  1. Compute gradient `g` of the current loss `L(θ)`. Estimate the worst-case perturbation `ε̂ = ρ · g / ‖g‖` within a neighborhood of radius `ρ`.
  2. Compute gradient `g̃` at the perturbed parameters `θ + ε̂`. Update `θ` using base optimizer (AdamW) with gradient `g̃`.
- **Objective minimised**: `min_θ max_{‖ε‖≤ρ} L(θ+ε)` — i.e., minimise the worst-case loss in a ρ-neighborhood.
- **Benefits**: Enforces smoothness in the learned embedding function; nearby inputs remain well-supported, preventing instruction blindness.
- **No extra data, no architectural modifications, no retraining** required.

### Experiments: Datasets, Scenarios, Benchmarks, Baselines

- **Simulation benchmarks** (three counterfactual instruction-following benchmarks):
  - LIBERO-PRO Task (40 tasks)
  - LIBERO-CF (50 tasks; includes OOD objects)
  - LangGap (59 tasks)
- **Real-world experiments**: five tabletop pick-and-place tasks with DROID setup; counterfactual instructions with/without background perturbation.
- **Baselines** (all finetuned on same LIBERO data unless noted):
  - Vanilla VLAs: OpenVLA-OFT, π0.5
  - Factorized/guidance methods: BayesVLA, CAG, π0.5 with classifier-free guidance (CFG)
  - Data-augmented: π0.5 LangGap (trained with more diverse language)
  - Representation regularization: π0.5 LoRA (adapter finetuning)
- **Metrics**: Task success rate; Keskar sharpness; maximum eigenvalue of Hessian (curvature); grounding rate (real-world).

### Compute / Resources

- **π0.5 SAM finetuning**: 4 × NVIDIA A100 (80GB). Hyperparameters: LR 5e-5, batch 16, ρ=0.075, 30k steps.
- **OpenVLA-OFT SAM finetuning**: 2 × NVIDIA H100 (80GB). Hyperparameters: LR 2e-4, batch 10, ρ=0.03, 150k steps.
- **Training time**: Not explicitly reported; overhead is ~2× forward passes per step compared to standard finetuning.

### Experiment Breadth: Runs, Ablations, Coverage

- **Number of runs**: 50 rollouts per task for all benchmarks (138 total tasks). Real-world: 10 rollouts per counterfactual task.
- **Ablations**:
  - Component-wise SAM (vision, language, action only) → global SAM outperforms.
  - ρ hyperparameter sweep (0.05, 0.075, 0.1).
  - Combination of SAM + inference-time guidance (CFG) → further improvement on some benchmarks.
- **Fair coverage**: Tests on object, spatial, goal, long-horizon, and OOD subsets; also real-world with background perturbations. Comparisons use same training data; additional-data methods still outperformed by SAM alone.

### Main Conclusions and Findings

- **Instruction following improves substantially**: +60.2% (LIBERO-PRO Task), +70.2% (LangGap), +217% (LIBERO-CF) over π0.5 baseline. Real-world: +163% success rate (13.8% → 36.3%).
- **Sharpness reduction verified**: SAM-finetuned models have lower sharpness (S) and smaller max Hessian eigenvalue.
- **Object grounding improves**: t-SNE visualisations show SAM preserves semantic clusters; object-token embeddings remain discriminative.
- **Global SAM > selective SAM**: Applying SAM to all components works best; individual component application does not improve over baseline.
- **SAM is complementary to guidance**: Combining SAM with CFG yields further gains on LIBERO-PRO Task (50.2% vs 42.6% with SAM alone), matching or improving other benchmarks.
- **OpenVLA-OFT also benefits**: SAM on a weaker architecture yields 107–219% improvement on simulation benchmarks.

### Strengths

- **Simple and practical**: No extra data, no architecture changes, no re-pretraining; directly replaces the optimizer during standard finetuning.
- **Strong empirical validation**: Three counterfactual benchmarks + real-world; 50 rollouts per task; multiple ablations.
- **Rigorous analysis**: Quantifies sharpness (Keskar metric, Hessian eigenvalues) and visualises embedding clusters; verifies the hypothesised mechanism.
- **Complementarity**: Works alongside inference-time guidance, provides a higher baseline for future work.
- **Reproducibility**: Hyperparameters and code details provided; uses publicly available models and benchmarks.

### Limitations

- **Compute overhead**: Two forward passes per step roughly doubles training time; memory requirements may be high for large models.
- **Absolute success rates remain low** (e.g., 42.6% on LIBERO-PRO Task, 36.3% real-world); instruction blindness is mitigated but not solved.
- **No explicit drift prevention**: SAM does not constrain weights near pretrained initialisation; methods like SWA or parameter merging could be more explicit.
- **Dataset bias is only partially addressed**: Sharpness reduction tackles overfitting, but fundamental limits (task complexity, data diversity) remain.
- **Generalizability to other VLA architectures**: Tested only on π0.5 and OpenVLA-OFT; may require ρ tuning for other models.

(End)
