---
title: Perturbation-Based Uncertainty for Failure Detection in Vision-Language-Action Models
authors: "Yousung Lee, Dongsoo Har"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20754v1"
tags: ["query:robotics"]
score: 9.0
evidence: Proposes label-free uncertainty estimation for VLA models to evaluate failure detection
tldr: "Vision-Language-Action models lack reliable uncertainty quantification under distribution shift. We propose perturbation-based uncertainty by injecting Gaussian noise into hidden activations to estimate epistemic signals. This label-free, model-agnostic method outperforms sampling-based approaches in failure detection on LIBERO and LIBERO-PRO, providing a practical uncertainty signal."
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20754v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 3584, \"height\": 1332}, {\"url\": \"assets/figures/arxiv/2606.20754v1/fig-002.webp\", \"caption\": \"\", \"page\": 4, \"index\": 2, \"width\": 1191, \"height\": 592}, {\"url\": \"assets/figures/arxiv/2606.20754v1/fig-003.webp\", \"caption\": \"\", \"page\": 4, \"index\": 3, \"width\": 2314, \"height\": 1954}, {\"url\": \"assets/figures/arxiv/2606.20754v1/fig-004.webp\", \"caption\": \"\", \"page\": 4, \"index\": 4, \"width\": 2314, \"height\": 1954}]"
motivation: "Existing uncertainty methods for VLA models rely on stochastic sampling or supervised failure labels, limiting their applicability across diverse pretrained models."
method: We inject Gaussian perturbations into transformer hidden activations and compute epistemic uncertainty from the disagreement among multiple perturbed action predictions.
result: Perturbation-based uncertainty consistently improves failure detection under distribution shift compared to sampling-based uncertainty on LIBERO and LIBERO-PRO benchmarks.
conclusion: "This work offers a practical, label-free uncertainty signal for VLA models, enabling reliable failure detection without requiring model modifications or training."
---

## Abstract
Vision-Language-Action (VLA) models have shown strong performance in robotic manipulation, but reliable uncertainty quantification remains challenging, particularly under distribution shift. Unlike autoregressive policies, many modern VLA models generate continuous actions through regression or flow-based generation, where explicit predictive probabilities are unavailable. Moreover, existing approaches often rely on stochastic action sampling or supervised failure labels, limiting their applicability across diverse pretrained VLA models. In this work, we propose a label-free and model-agnostic framework for inference-time uncertainty estimation through hidden activation perturbations, motivated by Bayesian perspectives on local model variations. Specifically, we inject Gaussian perturbations into transformer hidden activations and estimate epistemic signals from disagreement across perturbed action predictions. Experiments on LIBERO and LIBERO-PRO show that perturbation-based uncertainty consistently improves failure detection under distribution shift compared to sampling-based uncertainty, providing a practical uncertainty signal for VLA models.

---

## Detailed Summary (auto-generated)

# Perturbation-Based Uncertainty for Failure Detection in Vision-Language-Action Models

## 1. Core Problem and Motivation

- **Core problem**: Vision-Language-Action (VLA) models for robotic manipulation often fail catastrophically under distribution shift, yet they can remain overconfident. Reliable uncertainty quantification—especially epistemic uncertainty caused by limited model knowledge—is needed for safe deployment.
- **Existing limitations**: Many VLA models generate continuous actions via regression or flow-matching, not token probabilities, so autoregressive uncertainty methods do not apply. Current approaches either rely on stochastic action sampling (which captures aleatoric rather than epistemic uncertainty) or require supervised failure labels, which are costly and unsafe to collect.
- **Goal**: Propose a label-free, model-agnostic inference-time method that estimates epistemic uncertainty through hidden activation perturbations, thereby enabling failure detection under distribution shift without retraining or architecture modifications.

## 2. Methodology

- **Core idea**: Inject Gaussian noise into the transformer hidden activations of the vision or vision-language backbone. Each perturbed forward pass yields a different action chunk. Epistemic uncertainty is then measured as the variance across these perturbed predictions.
- **Technical details**:
  - Perturbations are applied to intermediate MLP activations shared across layers, approximating local parameter variations around pretrained weights \( \theta^* \):
    \[
    \theta^{(k)} = \theta^* + \epsilon^{(k)},\quad \epsilon^{(k)} \sim \mathcal{N}(0,\sigma^2 I)
    \]
    In practice, only hidden activations are perturbed to avoid computationally expensive full-parameter perturbation.
  - For each observation \( x_t \) (visual \( v_t \) and language instruction \( \ell \)), \( K \) perturbed action chunks \( a^{(k)}_{t:t+H-1} \) are generated.
  - The per-timestep uncertainty score is:
    \[
    u_t = \frac{1}{Hd} \sum_{h=0}^{H-1} \sum_{i=1}^{d} \text{Var}_k\left[a^{(k)}_{t+h,i}\right]
    \]
  - To stabilize detection, a temporal sliding window aggregates recent uncertainty:
    \[
    U_t = \frac{1}{T_w} \sum_{\tau = t-T_w+1}^{t} u_\tau
    \]
  - A trajectory-level score takes the maximum over \( U_t \); failure is declared if it exceeds a threshold.
- **Key design choice**: The same Gaussian noise tensor is reused across timesteps per perturbation sample, enforcing temporally consistent local model variations.

## 3. Experiments

- **Datasets and scenarios**: LIBERO (standard manipulation) and LIBERO-PRO (controlled distribution shifts in object positions and visual environments). Eight benchmark suites are used (e.g., Spatial-Pos, Object-Pos, Goal-Env).
- **Models tested**:
  - **π0.5**: flow-matching generative policy (supports stochastic action sampling).
  - **OpenVLA-OFT**: deterministic regression head.
- **Baseline**: Sampling-based uncertainty (multiple action chunks from different initial noise samples, without perturbation).
- **Metrics**: Area Under the ROC Curve (AUC) for trajectory-level failure detection, both full-episode and early (first 70% of episode length).
- **Results**:
  - Perturbation-based uncertainty consistently outperforms sampling-based uncertainty across all LIBERO-PRO benchmarks for π0.5. Largest gain on Object-Pos (+0.169 AUC).
  - Early failure detection also improves; e.g., Goal-Env early AUC 0.828 vs 0.681.
  - Qualitative example shows perturbation-based uncertainty spiking during failure-relevant manipulation, while sampling-based remains flat.
- **Ablation studies** (on LIBERO-10, 94% success rate):
  - Perturbation strength \( \sigma \): optimal around 0.25 for π0.5, 0.1 for OpenVLA-OFT; too large degrades performance.
  - Number of samples \( K \): as few as 2 samples yield comparable AUC (~0.869) to \( K=5 \) (~0.871); sampling-based uncertainty does not consistently improve with more samples.

## 4. Compute/Resources

- **Not reported**: The paper does not mention GPU types, counts, or training time. Since the method is inference-time only and uses a small number of extra forward passes (K=2–5), it is likely lightweight, but no explicit resource usage is provided.

## 5. Experiment Breadth

- **Coverage**: Eight LIBERO-PRO benchmarks covering both position shifts and environment shifts; two distinct VLA architectures (generative and deterministic). 500 episodes per benchmark.
- **Ablations**: Perturbation strength, number of samples, comparison to sampling baseline, early vs full trajectory detection. Missing: comparison to alternative epistemic methods (e.g., MC Dropout, ensembles), evaluation on in-distribution data, or real-robot validation.
- **Fairness**: The baseline (sampling-based) is a natural competitor for π0.5, but OpenVLA-OFT cannot be evaluated with sampling. For OpenVLA-OFT, only perturbation-based results are shown, limiting a direct comparison. Overall, the coverage is sufficient but not exhaustive.

## 6. Main Conclusions and Findings

- Perturbation-based uncertainty provides a practical, label-free signal for failure detection in pretrained VLA models under distribution shift, outperforming sampling-based uncertainty.
- The method captures epistemic uncertainty missed by stochastic action sampling, especially during failure-relevant phases.
- It works with very few samples (K=2) and a moderate perturbation magnitude, making it efficient and model-agnostic.
- The approach is applicable to both deterministic regression policies and generative flow-matching policies without requiring output probabilities.

## 7. Strengths

- **Label-free**: No need for costly failure trajectory collection or supervised training.
- **Model-agnostic**: Does not rely on specific architecture features (e.g., autoregressive tokens, stochastic decoding); works for regression and flow-based VLA models.
- **Low overhead**: Only a handful of extra forward passes are needed; perturbation is applied to hidden activations, not full model weights.
- **Early detection**: Temporal aggregation with moderate window sizes catches uncertainty before task failure.
- **Principled motivation**: Grounded in Bayesian decomposition of uncertainty, approximating epistemic uncertainty via local model variations.

## 8. Limitations

- **Tuning requirement**: Perturbation strength \( \sigma \) must be calibrated per model (e.g., 0.25 for π0.5, 0.1 for OpenVLA-OFT). No automatic selection method is provided.
- **Limited model coverage**: Tested on only two VLA models (π0.5 and OpenVLA-OFT) and two benchmarks (LIBERO, LIBERO-PRO). Generalizability to other VLA families (e.g., RT-2, Octo) or real-world sim-to-real scenarios is unverified.
- **No real-robot validation**: All experiments are in simulation; sim-to-real transfer or hardware deployment is not addressed.
- **No comparison to other epistemic methods**: Baseline is only sampling-based; alternative epistemic approximations (e.g., MC Dropout if applicable, deep ensembles) are not compared.
- **Trajectory-level only**: The method outputs a single failure flag per rollout; per-step or per-action failure detection is not evaluated.
- **Potential bias in perturbation location**: Perturbations are applied only to vision-language backbone activations, not to action-specific modules. The paper does not explore other perturbation sites (e.g., action head).

(End)
